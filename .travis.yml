language: python

cache: 
 - pip
 - directories:
   - $HOME/downloads

before_install:
 - if [ ! -f $HOME/downloads/sampledata.zip ]; then
     wget -q -P $HOME/downloads/ http://blackrockmicro.com/wp-content/uploads/2016/06/sampledata.zip ;
   fi  
 - unzip $HOME/downloads/sampledata.zip sampleData/sampleData.ns4 -d $TRAVIS_BUILD_DIR/tests/downloads/
 - wget -q https://repo.continuum.io/miniconda/Miniconda3-latest-Linux-x86_64.sh -O miniconda.sh
 - bash miniconda.sh -b -p $HOME/miniconda
 - export PATH="$HOME/miniconda/bin:$PATH"
 - hash -r
 - conda config --set always_yes yes --set changeps1 no
 - conda update -q conda
 - conda create -q -n test-environment numpy scipy pyqt pytest pytest-cov sphinx sphinx_rtd_theme
 - source activate test-environment
 - pip install coveralls pytest-qt
 - pip install nibabel mne

install: 
 - pip install -e .

before_script:
 - export DISPLAY=:99.0
 - sh -e /etc/init.d/xvfb start
 - sleep 3

script:
 - sphinx-build -b html -d $TRAVIS_BUILD_DIR/docs/build/doctrees $TRAVIS_BUILD_DIR/docs/source $TRAVIS_BUILD_DIR/docs/build/html
 - pytest --cov=phypno tests/

after_success:
 - coveralls

notifications:
 email: false

deploy:
 - provider: pages
   skip_cleanup: true
   target_branch : master
   local_dir : $TRAVIS_BUILD_DIR/docs/build/html
   public_repo : phypnodocs
   github_token : $GITHUB_TOKEN
 - provider: pypi
   user: $PYPI_USER
   password: $PYPI_PASSWORD
   on:
     tags: true
